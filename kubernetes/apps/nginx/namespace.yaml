---
apiVersion: v1
kind: Namespace
metadata:
  name: demo
  labels:
    # Pod Security Admission. restricted is the strictest profile and the
    # right default for an application namespace.
    pod-security.kubernetes.io/enforce: restricted
    pod-security.kubernetes.io/audit: restricted
    pod-security.kubernetes.io/warn: restricted
